import { rhythm, scale } from '../../utils/typography'
import styled from 'styled-components'

const P = styled.p`
  display: block;
  margin-bottom: ${rhythm(1)};
  margin-top: ${rhythm(-1)};
`

export default P
